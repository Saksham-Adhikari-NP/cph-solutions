#include<bits/stdc++.h>
using namespace std; 
int main () 
{
     int t;
     cin >> t;
    while(t--)
    {
        string b; 
        cin >> b;
        for(int =0 ; i < b.size(); i++)

        {
            if( int  > 2 && i % 2 = 0)  
            {
                continue;
            }
            else {
                cout << b[i];
            }
        }
    }
}