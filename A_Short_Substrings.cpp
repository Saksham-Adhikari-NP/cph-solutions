#include<bits/stdc++.h>
using namespace std; 
int main () 
_{
     int t;
     cin >> t;
    while(t--)
    {
        string b; 
        cin >> b;
        for(i=0 ; i < b.size(); i++)

        {
            if( i > 2 && i % 2 = 0)  
            {
                continue;
            }
            else {
                cout << b[i];
            }
        }
    }
}